package examples;

public class URLStringParser {
	public URLFormatObject parseURL(String url) throws InvaliedURLFormat{
	       String url=url;

	       String[] splits= url.split("/");
	       if (splits.length=4) {
	       	S
	       	
	       }
}
}