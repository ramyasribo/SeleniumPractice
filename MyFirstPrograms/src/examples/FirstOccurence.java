package examples;

public class FirstOccurence {
	

		public char finder(String word){

			int[] count = new char(word.length);
			for(int i=0;i<count;i++){
				result[i] = word.charAt(i);
				//result[0] = word.charAt(0);
			}
			return result;

		
	}

}
